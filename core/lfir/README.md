# Arkhe Core — LFIR

This module contains the canonical Linear Functional IR (LFIR) implementation.

## Purpose

- Provide a shared representation for coherent transformations
- Enable cross-language canonical types via FFI/protobuf
- Serve as the foundation for runtime compilation and verification
