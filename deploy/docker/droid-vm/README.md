# DroidVM Docker Deployment

This Docker setup allows running Android workloads in Docker using DroidVM, providing GPU acceleration support, ADB access, and suitability for CI pipelines.

## Build the image
```bash
docker-compose build
```

## Run the container
```bash
docker-compose up -d
```
