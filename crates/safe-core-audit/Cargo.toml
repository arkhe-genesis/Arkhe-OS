[package]
name = "safe-core-audit"
version = "1.0.0"
edition = "2024"
description = "Auditoria Imutável — Cadeia de Merkle para Decisões de Governança"

[dependencies]
serde = { version = "1.0", features = ["derive"] }
serde_json = "1.0"
chrono = { version = "0.4", features = ["serde"] }
blake3 = "1.5"
thiserror = "2.0"
