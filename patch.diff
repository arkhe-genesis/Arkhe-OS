--- substrates/630-PAPERDEBUGGER-BRIDGE/AcademicWritingAttestationBridge.sol
+++ substrates/630-PAPERDEBUGGER-BRIDGE/AcademicWritingAttestationBridge.sol
@@ -321,10 +321,7 @@
         if (totalRevisions == 0) {
             avgPhiScore = 0;
         } else {
-            uint256 totalPhi = 0;
-            for (uint256 i = 0; i < totalRevisions; i++) {
-                totalPhi += revisions[allRevisions[i]].phiScore;
-            }
-            avgPhiScore = totalPhi / totalRevisions;
+            // Calculate using a maintained state variable `totalPhiScore` incremented during anchorRevision
+            avgPhiScore = totalPhiScore / totalRevisions;
         }
     }
