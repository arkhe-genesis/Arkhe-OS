[package]
name = "substrate-9030"
version = "0.1.0"
edition = "2021"

[dependencies]
# ZK dependencies, maybe later

nexus-sdk = { workspace = true, optional = true }
nexus-core = { workspace = true, optional = true }
nexus-network = { workspace = true, optional = true }
