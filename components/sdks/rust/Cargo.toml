[package]
name = "arkhen-sdk"
version = "0.1.0"
edition = "2021"
description = "Rust SDK for Arkhe(n) Hybrid Architecture (ℂ × ℤ → ℝ⁴)"
authors = ["Hyperspace AI"]

[dependencies]
reqwest = { version = "0.11", features = ["json"] }
serde = { version = "1.0", features = ["derive"] }
serde_json = "1.0"
tokio = { version = "1", features = ["full"] }
