name: CI
on: [push]
jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v2
        with:
          lfs: true
      - name: Pull LFS objects
        run: git lfs pull
      - name: Run tests
        run: make test
