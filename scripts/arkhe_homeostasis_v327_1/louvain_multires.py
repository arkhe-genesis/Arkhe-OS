import numpy as np

def detect_communities_multires(J, resolution_range, min_community_size, cohesion_threshold):
    # Mock return
    return {
        'best_resolution': 0.5,
        'selected_communities': [
            {'is_coherent': True, 'crystals': list(range(50)), 'rho': 0.85}
        ]
    }
