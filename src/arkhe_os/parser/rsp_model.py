import json
import numpy as np

class ReciprocalSpaceNN:
    def __init__(self, k_mesh):
        self.k_mesh = k_mesh

def load_model_config(filepath):
    # Dummy implementation for testing
    return ReciprocalSpaceNN(k_mesh=np.random.rand(512, 3))