[package]
name = "arkhe-arxia-bridge"
version = "0.1.0"
edition = "2021"

[dependencies]
# Dependencies added as per Substrate 157 description
# arxia-transport = "*"
# arxia-gossip = "*"
# arxia-finality = "*"
serde = { version = "1.0", default-features = false }
postcard = { version = "1.0", default-features = false }

[features]
default = ["no_std"]
no_std = []
