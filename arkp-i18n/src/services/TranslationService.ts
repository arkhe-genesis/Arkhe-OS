export const TranslationService = {
    loadAutoTranslations: async (lang: string) => {},
    enableAutoTranslation: async (lang: string) => {},
    translate: async (term: string, source: string, target: string, opts: any) => term
};
