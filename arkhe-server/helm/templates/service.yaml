apiVersion: v1
kind: Service
metadata:
  name: {{ include "arkhe-server.fullname" . }}
  labels:
    {{- include "arkhe-server.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "arkhe-server.selectorLabels" . | nindent 4 }}
